interface:
  display_name: "Simplify"
  short_description: "Clean up changed code without changing behavior"
  default_prompt: "Use $simplify to clean up the current change for reuse, simplicity, efficiency, and abstraction altitude."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Standup"
  short_description: "Turn this session into a status checklist"
  default_prompt: "Use $standup to show the current session ledger and recommended next move."

policy:
  allow_implicit_invocation: false
